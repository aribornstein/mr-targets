// js/utils/utils.js
// Add any utility functions you may need (for example, random number generation,
// collision detection helpers, etc.)
export function randomInRange(min, max) {
    return min + Math.random() * (max - min);
  }
  